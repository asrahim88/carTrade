import { Container } from '@material-ui/core';
import React from 'react';

const Deals = () => {
    return (
        <Container fixed>
            <h1>Developer is sleeping so don't disturb him!</h1>
        </Container>
    );
};

export default Deals;